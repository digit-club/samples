chose port for server and client sockets

check it is free
# netstat -antu | grep <port>

set port to server class, compile and run 2 server and client separately

once server is up, print something and check that client reflects the print

inhanced programm could be:
- multiple clients (multithreading)
- chat
